# MolmoMotion-1M data-generation pipeline dependencies.
#   pip install -r requirements.txt
#   pip install -e third_party/sam3      # SAM 3
#   pip install -e third_party/vipe      # ViPE (compiles a CUDA extension; needs ninja + nvcc)
# AllTracker is used as plain source under third_party/alltracker (no install needed).

# PyTorch CUDA 12.8 wheels
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.0+cu128
torchvision==0.22.0+cu128

# Transformers / HuggingFace
# NOTE: MolmoPoint-Vid-4B / Molmo2 require transformers==4.57.1 (5.x produces garbage).
transformers==4.57.1
accelerate>=1.0.0
huggingface_hub>=0.25.0
safetensors>=0.4.0
tokenizers>=0.21.0

# Scientific
numpy==1.26.0
scipy>=1.15.0
scikit-learn>=1.8.0
scikit-image>=0.25.0
pandas

# SAM 3
pycocotools>=2.0.0

# Vision / IO
opencv-python-headless>=4.11.0
pillow>=11.0.0
imageio>=2.37.0
imageio-ffmpeg>=0.6.0
kornia>=0.8.0
timm>=1.0.24
einops>=0.8.0
decord>=0.6.0
av>=16.0.0
OpenEXR>=3.2.0

# ViPE config / CLI + CUDA build
hydra-core>=1.3.0
omegaconf>=2.3.0
click>=8.1.0
ninja

# Misc
matplotlib>=3.9.0
tqdm>=4.66.0
ftfy>=6.1.0
regex>=2024.0.0
iopath>=0.1.10
requests>=2.28.0
PyYAML>=6.0.0
